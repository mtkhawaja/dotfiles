# Global git ignores (referenced by core.excludesfile in ~/.gitconfig).
# Machine/OS/editor junk that should never be committed in ANY repo.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Python
*.pyc
__pycache__/

# Environment / secrets
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.secret
*.secrets
.vault-token

# Keys / certificates / keystores
*.pem
*.key
*.jks
*.keystore
*.p12
*.pfx

# Claude Code (machine-local / secret — never commit)
settings.local.json
.claude.json
.credentials.json
CLAUDE.local.md

# Logs
*.log
logs/
log/
